"ok"cat | ls ->  "ok"cat doit etre un seul token

(("ss))" -> doit etre une erreur de parsing car ce qui est dans les guillemets ne comptent pas

"" -> fait un invalid read 

-> revoir les guillemets

